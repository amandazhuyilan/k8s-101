apiVersion: v1
kind: apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    NodePort: 30008
  selector:
    app: myapp-service
    type: front-end
